  type Book {
    title: String
    author: String
  }

  
  type Query {
    books: [Book]
  }


  type Mutation {
  createDraft(title: String!, content: String):Post
  deletePost(id: ID!):Post
  publish(id: ID!):Post
}